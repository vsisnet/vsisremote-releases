# VsisRemote — bản phát hành Android

Repo công khai chứa APK + `android/manifest.json` cho tính năng **tự cập nhật trong app**.
App đọc `manifest.json`, so `versionCode`, tải APK asset rồi cài. Xem `tool/release_android.ps1` ở repo chính.
